$kind: http-request
url: "{{LOCAL_URL}}:{{PORT}}{{API_PREFIX}}/me/upload-avatar"
method: PATCH
headers:
  authorization: Bearer {{accessToken}}
body:
  type: formdata
  content:
    - type: file
      key: avatar
      src: ""
order: 2000
