import { Outlet } from "react-router-dom";

function AccountLayout() {
    return (
        <div>
            AccountLayout
            <Outlet />
        </div>
    );
}

export default AccountLayout;
